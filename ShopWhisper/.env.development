# ============ 开发环境共享配置 ============
# 此文件用于开发环境部署，请根据实际情况修改

# 环境标识
DEPLOY_ENV=development

# 主机IP地址（请修改为你的本机IP地址）
# 使用 ifconfig 或 ip addr 查看本机IP
# 例如：192.168.1.100, 192.168.0.105 等
HOST_IP=127.0.0.1
