# ============ 前端开发环境默认配置 ============
# 此文件包含开发环境的默认配置，可以提交到版本控制
# 本地开发时，创建 .env.local 文件覆盖这些默认值

# API Configuration
NEXT_PUBLIC_API_BASE_URL=/api/v1
NEXT_PUBLIC_WS_BASE_URL=ws://127.0.0.1/api/v1/ws
